# Byte-compiled / optimized Python files
__pycache__/
*.py[cod]
*.so
.Python

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
*.cover

# Tool caches
.mypy_cache/
.ruff_cache/
.hypothesis/

# Virtualenv
.venv/

# pip editable install artifacts
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp

# Archived docs (superseded by SPEC.md and README.md).
# Note: only affects UNTRACKED files; files already in history at this path
# stay tracked. To re-archive a new spec, git-mv it in rather than `git add`.
/docs/archive/

# Demo pipeline output — regenerated on each run
examples/test-dataset/output/
